# 交付前自检清单

生成后逐项过，全部通过才交用户审阅；能机检的错不留给用户。标注与脱敏的判定标准以 [walkthrough-guide.md](walkthrough-guide.md) 第四、五节为准，此处只列检查项。

## 两轨通用

- [ ] 骨架有据：`outline.md` 存在且经用户确认，正文章节与它一一对应，没有漏章也没有自作主张的增删。
- [ ] 流程走通：每个操作/配置路径在浏览器里从头到尾走通过，不是照口述或配置誊写。
- [ ] 章节完整：每章有"注意事项"和"常见问题"（配置手册为"注意事项"和"改动影响"）。
- [ ] 每个操作步骤序列有对应截图，回看截图核对文字描述，图文一致；步骤点名的按钮、字段、区域都能在图里找到（同构界面可引用前文图，正文写明）；选择、弹窗、审批面板等交互各有局部图，没有用一张表单全景代表整个填写过程。
- [ ] 截图与步骤所述路径一致：导航高亮落在步骤写的菜单上，不一致的重新导航重截。
- [ ] 标注框对得上所在步骤：框的范围只含步骤文字提到的内容（点一个按钮就只框那个按钮）；多框序号顺序与步骤文字一致，每个框都能在步骤文字里找到对应动作；无定位作用的框已删。
- [ ] 入口可达：菜单名对不上、两图之间有跳跃、外部平台控制台这三处挂了直达链接，普通菜单路径没挂；每个链接实际点开核过落点。
- [ ] 截图无可识别的真实客户敏感数据（演示数据不需要脱敏）；混入的真实信息已模糊，密钥/令牌/密码已不可逆遮挡；打过码的图按 walkthrough 第五节做过两遍复检（原值不可辨认、系统文字零遮挡）。
- [ ] **格式机检通过**：`python3 scripts/render.py <文档.md>` 退出码为 0，一条告警不留。
- [ ] 图片引用无死链：每个 `![](images/…)` 都在 images/ 里，且是相对路径（机检已覆盖，人工只补查 images/ 里的废图）。
- [ ] images/ 无未引用废图，`.orig.png` / `.grid.png` 中间产物已清理。
- [ ] 环境缺陷不入正文：按钮失效、异常引用等演示环境问题记在 notes.md 交付时报给用户，正文没有"当前工作区存在异常"式描述（内部行话已由机检覆盖）。
- [ ] 演示数据已按 demo-data.md 清理。

## 使用手册专项

- [ ] 字段讲解落在步骤里：必填在步骤点出且与表单实际必填一致；讲的都是有讲究的字段（默认值、显隐、计算、编号、关联带出、填错有后果）；全文无全字段表格。
- [ ] 无技术术语：正文无 trigger / automation / 数据触发 / config / API 等黑话。
- [ ] 去配置化：只写配置的表面行为，未写自动化触发条件、执行节点、审批流设计、显隐配置逻辑；计算字段公式保留。
- [ ] 自动化落地："点了之后发生什么"写进了对应位置。
- [ ] 业务流程模块册头有一张全流程图（各章开篇不配），图上表名与章节对应。

## 配置手册专项

- [ ] 第一章是成果展示（一张关键截图 + 一段话核心业务流与成果），无表清单和关系图。
- [ ] 章序即搭建顺序，只含涉及到的层。
- [ ] 每个配置单元先写"主要作用"再写内容。
- [ ] 每类配置有"入口在哪 + 编辑界面"两类截图。
- [ ] "改动会影响什么"有依据（引用查询或实测），拿不准的标了 `[待确认]`。
