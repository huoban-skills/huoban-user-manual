# Changelog

## 2026-08-24 阶段一停点硬化

- 删掉"一步一图不强求、至少三类图"那句：它给了偷懒口子，配图下限由元素落图、过程图按交互拆、长表单分屏三条规则界定。

- 删除「典型业务场景」册末固定章及其写法、机检（场景标题问法检查随之移除）。

- 盘点范围取表清单和导航的并集，导航无入口的表也列入，由用户确认纳入与否。

- 删除 CONTEXT.md 术语表，SKILL.md 与 README 的引用同步清理。

- 阶段一的交付物定义为 outline.md 本身：输出章节清单即任务正常完成，把"停下来"从中途等待改写成圆满交付。
- digest.py 加门闩：--outline 必传，outline.md 缺失或没有 `<!-- 用户已确认 -->` 标记就拒绝运行，报错文案直接指示"结束任务、交用户确认"。


## 2026-08-24 打码再简化

- 阶段〇补"切到目标工作区并核对"：按 space_id 查工作区名，切过去后 snapshot 核对，进错区不往下走；阶段一盘点起点固定为工作区首页，左侧导航滚到底看全再进表。

- 一律按演示数据处理，整册零打码，阶段一不再确认数据来源（outline.md 回到记三样）；唯一要打码的是 API key、密钥、账号密码类凭证，模糊仍不碰系统文字。


## 2026-08-24 机检加固（进销存版 codex 实测暴露的五个漏法）

- 机检新增：字段表列头必须是「字段 | 字段说明 | 填写说明」；册名合规（h1 不含"使用手册"/版本号/分隔符）、总览流程图必须在第一章之前、常见问题禁 Q/A 前缀、产出目录缺 outline.md 即报、元信息词表补"本手册/适用范围：/适用对象："。
- writing-guide：全册级主线和通识只住册头，不单开"使用前先看"类章节；走查规范补"截图前鼠标挪开，悬停提示不入图"。

## 2026-08-24 基础资料写法补全（参考金蝶基础资料章与用户样例）

- 基础资料模块固定含「基础资料导入说明」章（导入创建 / 导入修改 / 导入常见问题，按导入模板视图的真实机制写），各表只写导入特殊点。
- 通识小节（全模块通用）：必须先懂的机制各占一节，标题即机制名，放操作小节前。
- 场景介绍写法：第一句先说"是什么"；册头句式按模块类型分两种（业务流程按时间线、基础资料按维护顺序）。
- 基础资料章字段说明回归表格（字段 / 字段说明 / 填写说明，全字段按表单顺序），业务流程章维持揉步骤。
- 资料表特性场景各开一节（多单位、批次等，启用了才写）；分类小表排最前；期初类表涉及才写。


## 2026-08-24 排版调整

- 标注框六条收敛为四条：总纲改为"框 ↔ 步骤文字一一对应"，删掉按图类型划线的"非必要不画框"（与元素落图规则冲突：核对类步骤本就允许在结果图上标出字段位置）；出界条并入画后复核。
- walkthrough 第四五节重组：标注框的六条规则从「截图一致性」并入第五节并改名「标注框与脱敏」，第四节只留窗口尺寸和批次一致；删「脱敏操作顺序」节（前文复述）；SKILL.md 与 self-check 的指针改指第五节。

- 打码收敛为模糊一种：删掉不可逆遮挡档，要打码的一律 `--blur`。脚本的 `--fill` / `--mask` 参数保留备用，规范不再要求。

- 图注写两处：alt 之外，图片下方用 `<small style="color:#8A8F98">` 灰色小字再写一遍（md 编辑器不显示 alt，读者看不到图注；灰色小字与 HTML 的 figcaption 观感一致）；render.py 把图注行吸收成 figcaption 不重复输出，机检校验两处文字一致。
- 标注不出界：annotate.py 的框和角标自动内缩到画布内，browser.py 浮层角标同样内缩；render.py 机检检测图片边缘的标注色，越界即报。
- 小弹窗（确认框、提示框）连底层页面一起截整页，占大半屏的抽屉和选择器才截局部。

- 流程图改为一册一张：册头模块介绍后放全流程总览图（images/0-全流程总览.svg，图注不编号），各章开篇不再配图；机检对第一章之前的图豁免图号格式。
- 流程图整体缩小约四分之一（卡片、字号、间距），md 在任何编辑器打开都是小图。
- 常见问题改加粗编号行格式（**1. 问题？**），普通编辑器里不再是大标题；render.py 渲染成折叠块，旧式 #### 兼容。

## 2026-08-23 规则精简

- 机检新增"核对类步骤检查"：步骤点名多个字段（含查看/核对/确认/检查）时，小节内此后必须有配图或「如图 X-Y」引用；"确认无误后保存"类收尾句豁免。walkthrough 新增画后复核规则（框贴内容边界、不盖空白、错了从 .orig.png 重画）。
- 机检新增"缺框检查"：图注含入口/点击/按钮的图，扫像素验证标注框颜色存在（珊瑚色或旧版红），量了坐标不画框直接报；PIL 缺失时自动跳过。
- 机检新增两条（来自 codex 实测暴露的漏洞）：正文出现"走查/核验日期/本册适用于/本手册旨在"即报（内部行话与说明书腔元信息）；典型业务场景小节标题必须是问法（问号结尾）。self-check 补"环境缺陷不入正文"。

- 脱敏判定从"按字段类型"改为"按真实性"：演示工作区造的数据（演示单据、假客户、测试号码）不脱敏，只处理混入的真实客户信息；密钥类不分真假一律遮挡。此前按类型触发导致演示数据整列被打码。

- 字段写法金蝶化：取消字段表和"字段说明"小节，字段在填它的那一步里顺手讲（必填步骤点出、有讲究的多说一句、一看就懂的不提）；"唯一可选件"概念随之取消，定骨架不再问件。

- writing-guide 体检去重：字段说明可选件、审批说明、典型业务场景章、流程图位置各只说一处；骨架代码块补画册末章；"成册编排"小节解散并入骨架规则；前置条件规则移入「场景介绍写法」；修正骨架注释与注意事项写法的矛盾。
- 图文对应从"靠规范"改成"靠卡住"：写作步骤内嵌完成标准（每个操作序列三类图、逐步骤挂图、缺图回走查补截）；render.py 机检新增"操作小节有步骤没配图"告警。

- 回退多条来自客户反馈梳理的写作规则：区分必要与非必要步骤、重要步骤内嵌⚠️警示（含自检项）、少用简写、基础资料数据整理引导、导航无入口给路径。保留：不说"记录"、导航高亮与链接核对、业务流程章开篇流程图。
- 注意事项写法恢复为：系统硬边界 + 操作性提醒两条。
- 此前已删：混合模块类型、财务专项规则、模块级口径、章末警示分级、「开始前」小节。


## 2026-08-21 双轨改造 + 标注新样式 + 全套瘦身

### 双轨架构

- 单 skill 双模式：一次任务只出一本，使用手册（业务操作者，讲"我该做什么"）或配置手册（管理员，讲"在哪配、为什么、改了影响什么"），阶段一先定写哪本。
- 新增配置手册轨道（references/config-writing-guide.md）：第一章固定成果展示（一张关键截图 + 一段话核心业务流），之后按搭建顺序分章（建表与字段 → 自动化 → 审批流 → 权限角色 → 页面/工作台 → 数仓），涉及到的层才写；每个配置单元先说作用再讲配置，最后写改动影响；深度到"看懂 + 敢小改"。
- 新增 CONTEXT.md 术语表：使用手册 / 配置手册 / 成果展示章 / 序号角标等口径钉死。

### 使用手册轨道

- 形态统一为成册：砍掉"帮助中心单篇"和"混合"两种形态，单场景就是只有一章的册子。
- 可选件收敛：字段说明是唯一问项；审批说明、典型业务场景章固定保留；通用操作章删除。
- 模块类型收敛为二选一（基础资料 / 业务流程），删除混合类型和财务支撑型模块专项规则。
- 取消「开始前」小节：前置条件揉进章开篇的场景介绍，不单开一级。
- 骨架升级为整册骨架，并定义册头「模块介绍」写什么：管什么业务（含边界）、谁在用、业务主线；模块级口径也放册头讲清。
- 去配置化边界：只写配置的表面行为，自动化触发/执行节点、审批流设计、显隐配置逻辑归配置手册；计算字段公式保留。
- 业务流程章开篇配流程图：新增 scripts/flow.py（改自 huoban-automation-blueprint 的 render_flow.py），flow.json → SVG，步骤副行标表名，系统自动环节置灰虚线。
- 吸收客户反馈的写作规则：非必要步骤要说明、少用简写用大白话、不说"记录"说具体表单数据、重要步骤内嵌警示并分级（高危「⚠️」+ 正确做法）、财务类模块按业务视角展开、导航无入口给路径、基础资料引导用系统导入模板。

### 标注质量

- 标注框换柔和描边样式：珊瑚色（#D97757）圆角框 + 白色外圈 + 轻投影，annotate.py 与 browser.py --highlight 同款。
- 多框按传入顺序自动标 ①②③ 序号角标（单框不标），含义写在步骤文字里不入图；非必要不画框；相邻/同类内容合并成一个框。
- 脱敏选区规则反转：从"一值一小框"改为"系统文字禁入前提下合并模糊带"。
- 走查纪律新增：导航高亮必须与步骤路径一致，跳转链接实际点开核落点。

### 渲染与机检

- 修复目录双重序号：章标题自带"一、二、"，目录不再套 `<ol>` 自动编号。
- render.py 渲染后自动跑格式机检：章序号连续、小节编号对齐章号、图号图注格式、步骤序号连续、图片相对路径且存在；有问题逐条打印并以退出码 2 结束。self-check.md 增加对应机检项。

### 按 writing-for-agents 瘦身

- 标注与脱敏规则收敛为唯一事实源（walkthrough-guide 第四、五节），writing-guide / config-writing-guide / annotate.py 只留指针。
- 自检清单外置为 references/self-check.md（通用 / 使用手册 / 配置手册三段勾选清单）。
- SKILL.md 删掉 browser.py 命令表和 annotate.py 示例（以脚本头注释为事实源），18.8K → 11.8K。
- description 同义触发词合并并删除过期的"帮助中心"触发词，234 字 → 138 字。

## 2026-08-20

- 脱敏升级：普通敏感文字模糊、秘密不可逆遮挡，最小选区加两轮复检。

## 2026-08-13

- 不要把读者预设成顾问或实施方。
- 入口不好找就给链接，不能只靠截图。
- annotate 坐标系统一到成图，并强制先量后画；沉淀走查踩坑（SPA 坐标点击、标注量位置、原图备份）。

## 2026-08-12

- 走查一律用 browser.py，不接管用户日常浏览器；明确截图代劳边界。

## 2026-08-11

- 初版：浏览器实测版图文手册 skill；走查规范、窗口自适应、render.py HTML 预览。
