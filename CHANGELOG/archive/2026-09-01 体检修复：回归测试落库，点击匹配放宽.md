# 2026-09-01 体检修复：回归测试落库，点击匹配放宽

skill-reviewer 体检后的修复轮：

1. 新增 `scripts/selftest.py`：25 项回归用例一条命令跑完，覆盖 v1.5~v1.7 全部机检规则；改脚本后必跑，不绿不提交。此前所有验证用例散落在会话临时代码里，仓库没有任何回归手段。
2. 修真实漏洞（selftest 第一轮就抓到）：点击语境匹配要求动词紧贴「」，而真实写法是"点行末的「核销应收」""点右上角「添加数据」"——hac 拦截、证据核对、新增按钮检查对这类主流写法整体失效。三处正则放宽为允许 6 字以内的方位间隔（以句读和引号断界），基线手册与 codex 迭代版复核无误伤。
3. `check_outline` 定位「章节清单」改认标题行，outline 正文先提到这四个字不再切错段。
4. SKILL.md 阶段一补 outline 章节清单格式示例（机检强制缩进列表，此前从未示例过该格式）；速查表 render 命令注明改版加 `--baseline`；「反缩水」长段列表化。
5. README 补覆盖度机检与 selftest.py 条目。

验证：selftest 25/25；codex 迭代版重跑报错全为预期类别（元信息、空壳 FAQ、骨架未细化），无新误伤。
