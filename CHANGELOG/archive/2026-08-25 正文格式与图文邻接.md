# 正文格式与图文邻接

> 2026-08-25

- 正文一律标准 Markdown（标题 ## / ###、步骤 1. 2. 3.、要点 -），只有图注行用 `<small>`；机检拦 `<h1~6>` `<ol>` `<ul>` `<li>` `<p>` `<div>`。HTML 标签写正文会让 md 编辑器整块灰底显示，图也挂不进步骤条目。
- 图挂载规则强化：图紧跟在它描述的那一步后面（列表项内缩进 4 空格），中间不隔别的步骤、段落或代码块。
