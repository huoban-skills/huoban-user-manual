# 2026-08-31 验收修复：证据审计两处误报与文档去重

v1.5/v1.6 改完跑 skill-reviewer 验收，修掉报出的问题：

## 脚本（两处会产生误报的缺陷）

1. `render.py check_evidence`：正文比对时 body 去了全部空白，目标词没去——「导入 Excel」这类带空格的按钮名永远失配，必然误报"框住了但正文没提"。目标词参与比对前同样去空白，报错信息保留原词。
2. `browser.py HIGHLIGHT_ON`：取词逻辑 `innerText || value` 会把已填值输入框的**演示数据值**（如「演示-客户A」）当界面原文写进证据，触发框↔正文误报。表单元素（input/textarea/select）改取 aria-label/placeholder，取不到置空由短词过滤跳过。

## 文档

- README：版本号"v1.2"过时改为指向 CHANGELOG 最新；notes.md"交付时可删"与 SKILL.md"交付前不删"矛盾，统一为保留；产出结构补 `.meta.json` 说明；机检特性描述补语义审计。
- config-writing-guide：补一句证据层指针（配置手册同样适用）。
- 去重：".meta.json 交付保留"从 4 处收到 3 处（删 walkthrough §二那句，保留结构定义、清理动作邻近、自检清单三处）；"hac 名≠界面词"walkthrough §七的解释句缩为指向 SKILL.md 事实来源分级。

## 验证

带空格目标词正反用例通过；v1.5 全套 fixture 无回归，exit=0。

## 验收遗留（未修，留待实测）

- 配置手册轨道未跑过语义审计回归：自动化列表条目名=配置名，hac_names 硬拦有误伤可能，下次出配置手册前拿存量册跑一遍机检看误报面。
