# 轻采集收敛为 collect.py，走查清单强制落盘

> 2026-08-26

- 新增 `scripts/collect.py`，替代已下线的 `hac table er-diagram-collect`。逐表跑 get-table / form-layout get / automation list，一条命令落齐 facts.json、layout-*.json、automation-*.json；SKILL.md 阶段二与 digest.py 的报错提示同步改引用。
- 走查点位清单强制落盘 `notes.md`，逐点位勾记。清单是"这章哪些界面看过、哪些没看"的唯一凭证，不落盘等于点位推导没做，漏掉的点位（尤其详情页子表这类要滚到底才看得到的）事后想不起来。
- `browser.py` 支持 `HB_INSTANCE` 多实例并行走查：独立 profile + 独立端口，两个会话不再互相把页面导航走。
- self-check 新增两条：实测来源逐节核（每个描述界面的小节都要能在 notes.md 找到对应勾记）；后果写到业务层。
